/*
//create a program that gets the name  of an animal from the user and adds it to an array. Your program will also output an animal name from the array.  

let myAnimals = ['dog', 'cat', 'horse', 'meerkat']

//Got the name of an animal from the user and adds it to the end of the array using an array method.

let animal = prompt('Enter the name of an animal')
let myLowerAnimals = animal.toLowerCase()
let myNewAnimals = myAnimals.push(animal)
alert(`The last animal in the array is ${myLowerAnimals}`)
*/