/*Has a function named calcAvgSquare that can calculate the average of 3 numbers times the square of the first number. The formula is: 
       ((num1 + num2 + num3)/3) * num12
*/

/*
function calcAvgSquare (num1, num2, num3) {
  return (((num1 + num2 + num3)/3) * Math.pow(num1,2))
  }
let firstNumber = Number(prompt("Enter your first number"))
let secondNumber = Number(prompt("Enter your second number"))
let thirdNumber = Number(prompt("Enter your third number"))

alert(`The answer is ${calcAvgSquare (firstNumber, secondNumber, thirdNumber)}.`)
*/